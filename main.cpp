#include <windows.h>

int WINAPI WinMain(HINSTANCE, HINSTANCE, LPSTR, int)
{
    MessageBoxA(
        NULL,
        "CursorOverlay test",
        "CursorOverlay",
        MB_OK
    );

    return 0;
}
